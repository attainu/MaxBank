{
  "name": "server",
  "version": "1.0.0",
  "description": "server",
  "main": "server/index.js",
  "engines": {
    "node": "12.18.3"
  },
  "scripts": {
    "start": "node server",
    "build": "npm install && cd react-app/ && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-app/node_modules"
  ],
  "author": "rr",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "stripe": "^8.84.0"
  }
}
